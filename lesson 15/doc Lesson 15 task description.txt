Решить такую задачу
   1) Создать Хэш для описания животного с такими ключами
      type (хищник, травоядный) string
      kingdom (млекопитающее, пресмыкающиеся, паукообразные, рыбы)
      size (размер в сантиметрах)
      weight (вес в граммах)
      kind (название вида: слоны, обезьяны, акулы и тд)
      sex (пол )

    2) Создать переменную-образец с названием animal
    со всеми этими параметрами по нулям или пустыми если строки

    3) С помощью метода clone создать по 10 переменных-клонов
    этой переменной animal для каждого царства
    (млекопитающее, пресмыкающиеся, паукообразные, рыбы)
    5 женского пола 5 мужского например:
        spider_1 = animal.clone
        spider_2 = animal.clone и тд


    4) для каждого клона задать свои параметры например
    spider1[:type] = ‘хищники’
    spider1[:kingdom] = ‘паукообразные’
    spider1[:size] = 15
    spider1[:kind] = ‘тарантул’
    и тд.

    5) Создать массив animals и поместить туда все 40 клонов

    6) Решить такие задачи:
    Найти самого большого хищника и самого большого травоядного
    Найти самую длинную самку и самца
    Найти царство с самым большим представителем мужского пола и женского
    Найти вид с самым длинным названием и для этого вида найти самую крупную особь

    7)
    Добавить в класс методы для работы с файлами
    https://ru.wikibooks.org/wiki/Ruby/Работа_с_файлами

     - Считать данные с файла
     - Записать результат в файл
     - Добавить данные в файл
     !!! Условие тот файл который вы считываете
     и тот файл который вы записываете должен иметь один формат.
     То есть данные с любого файла можно загрузить
     в вашу программу и она правильно сработает.

      8) Создать меню
         вывод данных по запросу
         ввод запрос пользователя
      9) управляющий модуль для всех алгоритмов
      Модуль умеющий работать с всеми служебными модулями
       - Декоратором
       - Парсером(файлы)
       - Списком животны (AnimalList)


      10)Создть тех.документацию по задаче
      - описание модулей
      - схема структуры приложения
      - описание интнерфейсов
      - реализация проекта в коде


